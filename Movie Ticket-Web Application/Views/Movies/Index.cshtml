@model IEnumerable<AspNetCoreTodo.Models.Movie>

@{
    ViewData["Title"] = "Index-Movie Ticketing Web App-ComIT";
}



<div class="container">
<div class="row">
	<div class="col-sm-12">
        
    <div><button type="button" class="btn btn-success float-right --white"><a style="color:white"asp-action="Create">Create New</a></button><h3 class="mt-4 mb-4">Available Movies list table</h3>
    
    </div>
<table class="table table-striped table-hover"">
    <thead class="thead-dark">
        <tr>
             <th>
               Cover
            </th>
            <th>
                Movie Title
            </th>
            <th>
                Total Capacity
            </th>
            <th>
                Number of bookings
            </th>
           
            <th>
                Description
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td  style="width:25%">
               <img style="width:25%"src="@item.imgUrl" />
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TotalCapacity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BookedCapacity)
            </td>
            <td>
                <a type="button"  class="btn btn-primary" asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a type="button"  class="btn btn-info" asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a type="button"  class="btn btn-danger" asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

	</div>
</div>
</div>
